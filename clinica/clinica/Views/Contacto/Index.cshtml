@model IEnumerable<clinica.Models.Contacto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContactoEmergencia.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paciente.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContactoEmergencia.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paciente.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContactoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContactoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContactoID })
        </td>
    </tr>
}

</table>

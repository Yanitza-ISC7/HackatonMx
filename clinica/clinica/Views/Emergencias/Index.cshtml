@model IEnumerable<clinica.Models.Emergencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hospital.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paciente.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paramedico.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaEmergencia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hospital.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paciente.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paramedico.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaEmergencia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmergenciaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmergenciaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmergenciaID })
        </td>
    </tr>
}

</table>
